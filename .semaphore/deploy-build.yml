version: "v1.0"
name: "Fake Build"
agent:
  machine:
    type: "e1-standard-2"
    os_image: "ubuntu2004"

blocks:
  - name: "Fake build"
    task:
      jobs:
      - name: "Fake build it"
        commands:
          - "true"
          - "echo \"done\""
