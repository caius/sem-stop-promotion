version: "v1.0"
name: "Fake Build"
agent:
  machine:
    type: "e1-standard-2"
    os_image: "ubuntu2004"

blocks:
  - name: "Fake build"
    task:
      jobs:
      - name: "Fake build it"
        commands:
          - "curl -XPATCH --header \"Accept: application/json\" --header \"Content-Type: application/json\" --data '{\"terminate_request\": true}' https://swedishcampground.semaphoreci.com/api/v1alpha/pipelines/${SEMAPHORE_PIPELINE_ID}"
          - "true"
          - "echo \"done\""
